#include <bits/stdc++.h>
#include<iostream>
#include<set>
using namespace std;

int main()
{
  int N, M;
  cin >> N >> M;

  vector<int> A(N);
  for (int i = 0; i < N; i++){
    cin >> A[i];
    }
  vector<int> B(M);
    for (int i = 0; i < M; i++){
    cin >> B[i];
    }
  
  for (int j = 0; j < M; j++){
    int n = A.find(B[j])
    if (n != npos){
      
    }
  }
  cout << SA << endl;
}